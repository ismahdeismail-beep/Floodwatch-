# @floodwatch/eslint-config

Shared ESLint flat config (ESLint 9 + typescript-eslint) for all FloodWatch AI TypeScript apps and packages.

```js
// eslint.config.mjs in any app/package
import floodwatch from "@floodwatch/eslint-config";
export default floodwatch;
```

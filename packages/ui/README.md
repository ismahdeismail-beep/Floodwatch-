# @floodwatch/ui

Shared React UI components for FloodWatch AI — Tailwind-powered, dark-theme, zero runtime deps.

Components: `Button`, `Card`, `Badge`, `SeverityBadge`, `RiskBadge`, `Spinner`, `StatCard`, `PageHeader`.

```tsx
import { Button, Card, RiskBadge } from "@floodwatch/ui";
```

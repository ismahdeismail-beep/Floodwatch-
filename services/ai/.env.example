# ═══ FloodWatch AI — ai service configuration ═══
# Copy into the repo-root .env or provide as container env vars.

LOG_LEVEL=INFO
MODEL_DIR=../models/registry
MODEL_VERSION=v0.1-baseline
MLFLOW_TRACKING_URI=

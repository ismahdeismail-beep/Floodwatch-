# ═══ FloodWatch AI — auth service configuration ═══
# Copy into the repo-root .env or provide as container env vars.

LOG_LEVEL=INFO
JWT_SECRET=change-me-in-production
JWT_ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=60
REFRESH_TOKEN_EXPIRE_DAYS=7
